# UmojaHub Corrective Actions Checklist

**Source:** Architecture review chain over commit `dfc5d8c` — Decision Records 01-B (OAuth), 02-A (Onboarding Lock), 03/08-C (Group Token Join), Sections C/D/E directives (Q3–Q14), and the six-deliverable blueprint review.
**Protocol:** Work items top-to-bottom within a phase; phases are dependency-ordered. Check off (`[x]`) only when implemented AND `npm run type-check && npm run lint && npm run test` passes. Update the status line after every session.

**Status:** 51 / 51 complete ✅ · Started 2026-06-10 · Last update 2026-06-14 · **Phase 0 ✅** (DOC-01…07) · Phase 1 ✅ (PR #23) · Phase 2 ✅ (BE-01…09) · Phase 3 ✅ (AUTH-01…07) · Phase 5 ✅ (QA-01…04, 638 tests) on `feature/auth-onboarding-migration` · **Phase 4 ✅ COMPLETE: HARNESS-0 + UI-01…15 all shipped**

---

## PHASE 0 — Blueprint Document Corrections (v1.1)
*Paper fixes first: the six deliverables must stop contradicting the live API before any code is written against them.*

- [x] **DOC-01** ✅ (`23abe9d`) — Write corrected `context/APPLICATION_EXPERIENCE_ARCHITECTURE.md` v1.1: real order states only (`AWAITING_PAYMENT / IN_FULFILLMENT / COMPLETED`, no `DELIVERED`/`DISPATCHED`/`PAID_IN_FULFILLMENT`); reliability gate re-specified as **composite < 40 (NEW tier)**, flagged as a new backend rule; middleware whitelist extended to `/marketplace`, `/knowledge`, `/api/webhooks/daraja`, `/api/health`, `/api/transparency`, website routes; strike the SMS-delivery ticker (no data source) or log SMS-event-log prerequisite; Daraja wait = 90 s poll + timeout path, never an unbounded navigation lock.
- [x] **DOC-02** ✅ (`23abe9d`) — Write corrected `context/APPLICATION_USER_JOURNEYS.md` v1.1: handover window = **24 h** (matches `farmerTrustCalculator`); student journey = **3 hashed documents + 2 structured logs** (not five textareas); per-document hashes (3), no "compiled hash"; lecturer journey includes **4 mandatory ≥50-word comment fields**; buyer mediation 48-h gate recorded as a new product rule.
- [x] **DOC-03** ✅ (`23abe9d`) — Write corrected `context/APPLICATION_ONBOARDING_ARCHITECTURE.md` v1.1: field-by-field mapping to existing schema vs. net-new (full table from review §4); farmer Stage 3 reuses `verificationStatus: PENDING` + existing admin queue; buyer KYC ruling applied (queue or drop the cert upload — see BE-09); county enum only (no sub-county).
- [x] **DOC-04** ✅ (`23abe9d`) — Write corrected `context/APPLICATION_DESIGN_SYSTEM.md` v1.1 with jurisdiction preamble: *`context/FRONTEND.md` governs the public website; this document governs the authenticated app shell exclusively.*
- [x] **DOC-05** ✅ (`23abe9d`) — Write corrected `context/APPLICATION_SCREEN_INVENTORY.md` v1.1: add the **entire Admin section** (farmer/lecturer/supplier verification queues, Knowledge CMS, brief contexts, impact summary, withdrawal-request queue, mediation queue, group-token console); add Farm Assistant, price alerts, AI Mentor, Portfolio, peer-review queue list, settings pane w/ "Link Institutional Group Token".
- [x] **DOC-06** ✅ (`23abe9d`) — Write corrected `context/APPLICATION_FIGMA_BLUEPRINT.md` v1.1: VIEW-DET-01 redrawn (3 document editors + 2 structured-entry log tables, checklist 3/3, three hash strings); lecturer workspace comment fields added; variables table paths fixed (`src/lib/models/*.model.ts`, `src/components/shared/Sidebar.tsx`); `DataGrid.tsx`/`TableComponents.tsx` marked net-new.
- [x] **DOC-07** ✅ (`23abe9d`, written as `context/DECISION_RECORD_03-08-C.md`) — Amend Decision Record 03/08-C: field is `createdBy` (not `manager`); strike "absence of membership endpoints" rationale (ADD endpoint exists, atomic); strike "regional cooperative analytics feed" (no data source) → own-group read-only view; strike per-member "active flag"; append GroupJoinToken prerequisite list.

## PHASE 1 — Existing-Code Defect Fixes (small, high-leverage, no new collections)

- [x] **FIX-01** — `GET /api/orders` farmer view: `canConfirmDispatch = paymentStatus === 'PAID' && !confirmedByFarmerAt` (replaces dead `AWAITING_PAYMENT` check). File: `src/app/api/orders/route.ts:173–175`. ✅ Done 2026-06-11, commits `be8ce6f` + `cacae0b` on `fix/farmer-dispatch-confirm-flag`; gate passed (tsc clean, lint 0 errors, 452/452 tests). Scope note: also hardened the PATCH status route — IN_FULFILLMENT transition now requires the order to currently be IN_FULFILLMENT, preventing regression of COMPLETED/DISPUTED orders once the button became visible.
- [x] **FIX-02** — Public marketplace page reads `{ data, nextCursor, total }` (not `listings`). File: `src/app/marketplace/page.tsx`. ✅ Done 2026-06-11, commit `b9dcc7f`; gate passed (tsc clean, lint 0 errors, 452/452). Scope note: full DTO mismatch, not just the key — added typed `toCardData` adapter mapping `id→_id`, `imageUrl→imageUrls[]`, `farmer.isVerified→isVerifiedListing`, flat trust fields → nested `trustScore` object. API contract untouched.
- [x] **FIX-03** — Listing detail page reads `data.data` (not `data.listing`). File: `src/app/marketplace/[listingId]/page.tsx`. ✅ Done 2026-06-11, commit `a266c1e`; gate passed (tsc clean, lint 0 errors, 452/452). Scope note: also guarded the nullable `farmer` object (API returns null for unresolvable accounts; page dereferenced it unguarded incl. in generateMetadata) and **additively** enriched `GET /api/marketplace/[listingId]` to return `trustScore.ratingScore.{averageRating,totalRatings}` from the existing FarmerTrustScore fields — the page's "x.x avg (n orders)" line previously referenced data the API never sent.
- [x] **FIX-04** — Implement `own=true` on `GET /api/marketplace`: session-authed FARMER filter on `farmerId`, includes non-AVAILABLE statuses; fix farmer listings page response key. Files: `src/app/api/marketplace/route.ts`, `src/app/dashboard/farmer/listings/page.tsx`. ✅ Done 2026-06-11, commit `5bde6ce`; gate passed (tsc clean, lint 0 errors, 452/452). Notes: own branch is isolated from public filters, `_id`-cursor paginated, 401/403 via `requireRole`; jest coverage for the branch deferred to QA-01 per protocol. Page's Pause/Reactivate toggle remains a server no-op until **FIX-05**.
- [x] **FIX-05** — Farmer listing activate/deactivate: accept `listingStatus` (AVAILABLE|INACTIVE only) on owner PATCH via explicit schema extension (not `cropListingSchema.partial()` passthrough). Files: `src/lib/validation/farmerSchema.ts`, `src/app/api/marketplace/[listingId]/route.ts`. ✅ Done 2026-06-11, commit `76092d6`; gate passed (tsc clean, lint 0 errors, 459/459 — +7 new `listingUpdateSchema` tests, validation coverage bar respected). Scope notes: added reactivation stock guard (409 `LISTING_NO_STOCK` when returning to AVAILABLE with 0 effective stock); page toggle no longer applies optimistic state on failed responses. INACTIVE state is now reachable for the first time.
- [x] **FIX-06** — Enforce `farmerData.isVerified` on group member ADD and group-order JOIN (website-promised, unenforced). Files: `src/app/api/groups/[groupId]/route.ts`, `src/app/api/groups/[groupId]/orders/[orderId]/route.ts`. ✅ Done 2026-06-11, commit `d7675e7`; gate passed (tsc clean, lint 0 errors, 461/461). Scope note: gate also applied to group-order **PROPOSE** (`orders/route.ts`) — the proposer auto-joins as first participant, so JOIN-only enforcement left a bypass. All three paths 403 `FARMER_NOT_VERIFIED`. Pre-gate members who are unverified remain in rosters but cannot join/propose orders.
- [x] **FIX-07** — Lecturer register redirect target → `/dashboard/lecturer/queue` (current target `/dashboard/lecturer/reviews/pending` doesn't exist). File: `src/app/auth/register/page.tsx:43`. ✅ Done 2026-06-11, commit `96ee214`.
- [x] **FIX-08** — Strip `peerReviewId` populate from `GET /api/lecturer/reviews/[engagementId]`; expose peer scores via post-submission fetch. **RULING: accepted — supersedes the Q10 client-side mask directive.** ✅ Done 2026-06-11, commit `f3c0e79`; gate passed (tsc clean, lint 0 errors, 463/463, +2 tests). Implementation: detail GET excludes the field entirely (`-peerReviewId`); `POST /api/lecturer/reviews` returns `{ data, peerReview }` after the decision is recorded; `ReviewScoreForm.onSuccess(peerReview)` carries it; review page renders the section only post-decision with provenance text. The `/trust` independence claim is now server-enforced. **UI-10's mask requirement is void** — update UI-10 scope at build time.

## PHASE 2 — New Backend Prerequisites (models + routes the directives silently require)

- [x] **BE-01** — `WithdrawalRequest` model (farmerId, amountKES, status REQUESTED→APPROVED→PAID/REJECTED, requestedAt, resolvedBy/At, note). File: `src/lib/models/WithdrawalRequest.model.ts`. ✅ Done 2026-06-11, commit `a686572` on `feature/payout-requests` (stacked on Phase 1). Enum added to `src/types`.
- [x] **BE-02** — `POST /api/farmers/payout-requests` (FARMER; amount ≤ escrow balance derived from PAID/COMPLETED orders minus prior requests) + `GET` own history. ✅ Done 2026-06-11, commit `f47f9dc`; gate passed (tsc clean, lint 0 errors, 471/471, +8 payoutSchema tests). Notes: balance derived via new `computeEscrowBalance` (`src/lib/foodhub/escrow.ts`) — never stored; one open REQUESTED request at a time (409 `PAYOUT_REQUEST_PENDING`); over-balance → 409 `PAYOUT_INSUFFICIENT_BALANCE`; suspended accounts blocked. Helper is shared with BE-08 ledger endpoint. Route tests deferred to QA-01.
- [x] **BE-03** — Admin payout queue: `GET /api/admin/payout-requests` + `PATCH` decision route + `AdminAuditLog` actions `PAYOUT_APPROVED|PAYOUT_REJECTED|PAYOUT_MARKED_PAID`. ✅ Done 2026-06-11, commit `833e852`; gate passed (tsc clean, lint 0 errors, 478/478, +7 `adminPayoutDecisionSchema` tests). Notes: transitions atomic + status-guarded (409 `PAYOUT_INVALID_STATUS_TRANSITION` on race/wrong state); REJECTED requires reason note (Zod refine); PAID note carries manual M-Pesa ref; SMS to farmer on every decision; queue GET enriched with farmer identity + live `queueSize`. Admin `/api/admin/*` middleware gate applies automatically. Route tests deferred to QA-01.
- [x] **BE-04** — `MediationRequest` model (orderId, buyerId, farmerId, category, description, status OPEN→IN_REVIEW→RESOLVED, resolvedBy/At). ✅ Done 2026-06-11, commit `99b4481`; gate passed (tsc clean, lint 0 errors, 478/478). Notes: added `MediationRequestStatus` + `MediationCategory` (NOT_DELIVERED / QUALITY_ISSUE / WRONG_QUANTITY / OTHER) enums to `src/types`; `resolutionNote` field (≤500); **partial unique index on `{orderId}` where status=OPEN** backs the one-open-escalation-per-order rule at the DB layer; trust-score decoupling documented in the model header.
- [x] **BE-05** — `POST /api/orders/[orderId]/mediation` (BUYER, own order, ≥48 h in IN_FULFILLMENT, one open request per order) + admin `GET/PATCH` list. Does **not** touch order state machine; does **not** feed trust score (decoupling recorded). ✅ Done 2026-06-11, commit `e791cbe`; gate passed (tsc clean, lint 0 errors, 490/490, +12 mediation schema tests). Notes: 48 h anchored to `paidAt` (`MEDIATION_ESCALATION_HOURS` const, 409 `MEDIATION_TOO_EARLY`); re-filing blocked while OPEN **or** IN_REVIEW (409 `MEDIATION_ALREADY_OPEN`); GET on the order path serves the latest escalation to buyer/farmer/admin (powers UI-06's `UNDER_MEDIATION` bar); admin queue enriched with both parties + order ref; RESOLVED requires `resolutionNote` (Zod refine); audit actions `MEDIATION_IN_REVIEW`/`MEDIATION_RESOLVED`. Route tests deferred to QA-01.
- [x] **BE-06** — `GroupJoinToken` model (token, groupId, single-use, expiresAt, mintedBy, redeemedBy/At). ✅ Done 2026-06-11, commit `f4c62ee`; gate passed (tsc clean, lint 0 errors, 490/490). Notes: token stored uppercase w/ unique index; rows retained after redemption/expiry as the join audit trail (no TTL deletion); single-use enforced at redemption via atomic `redeemedBy: null` guard (BE-07).
- [x] **BE-07** — Admin mint route `POST /api/admin/group-tokens` (+ Africa's Talking SMS dispatch) and farmer redeem route `POST /api/groups/redeem-token` — redemption internally reuses the existing `$addToSet`/`$inc` write path and enforces FIX-06's verification gate. ✅ Done 2026-06-13, commit `3bdd793`; gate passed (tsc clean, lint 0 errors, 505/505, +15 group token schema tests). Notes: mint generates an 8-char unambiguous uppercase code (no 0/O/1/I/L) with a bounded re-roll on the unique-index collision; admin-tunable expiry (1 h…30 d, default 14 d); audit action `GROUP_TOKEN_MINTED`; SMS non-blocking. Redeem normalises to uppercase, pre-checks not-found/redeemed/expired (404/409/410), then claims atomically on `redeemedBy: null` before the roster `$addToSet`/`$inc`; ALREADY_MEMBER and member-cap guarded before the claim so a wasted token is never consumed. Route tests deferred to QA-01.
- [x] **BE-08** — Escrow ledger read endpoint `GET /api/farmers/ledger`: per-order PAID/COMPLETED line items + derived balance (gross received − payout requests). Powers SCR-FAR-004. ✅ Done 2026-06-13, commit `5930f05`; gate passed (tsc clean, lint 0 errors, 505/505). Notes: line items are the credit side only — orders with `paymentStatus: PAID` (the sole writer of gross), selected lean and capped at the 100 most recent by `paidAt`; each carries `fulfillmentStatus` so the farmer sees IN_FULFILLMENT/RECEIVED/COMPLETED progress. Balance reuses `computeEscrowBalance` and is computed independently over the full set, so line-item truncation never affects totals. Read-only, no request body → no validation schema; route tests deferred to QA-01.
- [x] **BE-09** — **RULING: option (a) — buyer-verification admin queue.** ✅ Done 2026-06-13, commit `c8121d5`; gate passed (tsc clean, lint 0 errors, 516/516, +11 buyerSchema tests). Implementation mirrors the farmer verification trio: added a `buyerData` sub-document (`verificationStatus`/`isVerified`/`taxComplianceCertificate`) + `buyerData.verificationStatus` index to the User model (AUTH-01 extends it with onboarding identity fields); `POST /api/buyers/verify` (BUYER submits KRA cert → PENDING, blocks re-submit while PENDING/APPROVED); `GET /api/admin/buyer-verification-queue` (cursor-paginated PENDING buyers); `PATCH /api/admin/verify-buyer` (PENDING→APPROVED|REJECTED, flips `isVerified`, **no trust score** unlike farmers, audit `BUYER_APPROVED`/`BUYER_REJECTED`, SMS). DOC-03/AUTH-06 buyer Stage 3 now unblocked — the onboarding cert upload calls `POST /api/buyers/verify`. Route tests deferred to QA-01.

## PHASE 3 — Auth Migration (Decisions 01-B + 02-A, executed together)

- [x] **AUTH-01** — User schema expansion: `onboardingStage` enum (`ROLE_SELECTION|IDENTITY_INPUT|VERIFICATION_UPLOAD|COMPLETED`), `oauthProvider`, nullable `role`; net-new sub-doc fields per DOC-03 table (**`buyerData` sub-document** — organizationName, businessRegistrationNumber, corporatePaybill, procurementScale; `studentData.institutionalEmail` + `institutionalEmailVerified` + `academicRegistrationNumber`; `lecturerData.departmentAssignment` + `academicStaffId` + `facultyCredentialLetterUrl`; `farmerData.landOwnershipToken`). ✅ Done 2026-06-13, commit `cf2ed90` on **`feature/auth-onboarding-migration`** (Phase 3 branch, stacked on `feature/payout-requests`); gate passed (tsc clean, lint 0 errors, 516/516). Notes: additive only — no behavioural change, no new tests (schema, not validation). `role` is nullable via `enum: [...Object.values(Role), null]`, default null; `onboardingStage` defaults to **COMPLETED** so every pre-OAuth creation path (credentials register, seeds) stays onboarded — **AUTH-02 must explicitly write the starting stage on OAuth account creation.** `buyerData` was extended in place (not recreated) on top of BE-09's verification fields. `IUserDocument.role` widened to `string | null` with zero call-site fallout. `OnboardingStage`/`OAuthProvider` enums added to `src/types`.
- [x] **AUTH-02** — NextAuth: Google + GitHub providers; `signIn` callback (suspension check moved from `authorize`, GitHub verified-email requirement via `user:email` scope, provider↔role enforcement, account-linking-by-verified-email policy); `jwt`/`session` callbacks carry `role`, `isOnboarded`, `onboardingStage`, `isVerified`; token refresh on onboarding completion (`update()` trigger). ✅ Done 2026-06-13, commit `72a8ce3`; gate passed (tsc clean, lint 0 errors, 516/516). **User rulings:** provider↔role = **GitHub→STUDENT only, Google→all other roles**; account-linking = **block-and-redirect** (never auto-link; an email already tied to another provider/credentials returns `?error=AccountExists`). Implementation: suspension moved to `signIn` for all providers (email-verified stays in `authorize`, credentials-specific); Google email via `profile.email_verified`, GitHub via the `/user/emails` API (scope `user:email`) → `?error=OAuthEmailUnverified` on failure; new OAuth users created as partial `ROLE_SELECTION` accounts (role null), GitHub login captured as `studentData.githubUsername`; existing-user provider mismatch → `?error=ProviderRoleMismatch`. `jwt` hydrates id/role/firstName/onboardingStage/isOnboarded/isVerified from the DB on initial sign-in and on `trigger==='update'`. `isVerified` is role-specific (farmer/buyer/lecturer `isVerified` flag). Schema: `lastName`/`phoneNumber`/`county` relaxed to optional for partial OAuth users (Zod `registerSchema` still enforces them on credentials). `requireRole` param widened to `role?: string \| null`. Added 4 OAuth env vars. Auth smoke tests deferred to QA-03. **AUTH-05 owes:** role-selection must constrain selectable roles by provider (GitHub→STUDENT), and move/confirm `githubUsername`.
- [x] **AUTH-03** — Admin bootstrap: Google-email allowlist (env `ADMIN_EMAIL_ALLOWLIST`) mapped to ADMIN in `signIn`; retire password-based `seed-admin.ts` path. **Deploy gate: must land in the same release that removes CredentialsProvider.** ✅ Done 2026-06-13, commit `7bdaa18`; gate passed (tsc clean, lint 0 errors, 516/516). Notes: `getAdminAllowlist()` parses comma-separated lowercased emails from `ADMIN_EMAIL_ALLOWLIST` (optional, **not** in `requiredEnvVars` — empty = no allowlisted admins, read via `process.env`). Applied at **new-OAuth-user creation only**, Google-only: an allowlisted Google email is created as `role: ADMIN` + `onboardingStage: COMPLETED` (skips the funnel); GitHub is never an admin path. Existing users are not auto-promoted on login (bootstrap is creation-time). Deleted `scripts/seed-admin.ts` (no package.json/test references; PRODUCTION_ROADMAP still mentions it — doc cleanup out of scope). Deploy gate stands: this branch removes CredentialsProvider in AUTH-07, so they ship together. Smoke test (allowlist bootstrap) deferred to QA-03.
- [x] **AUTH-04** — Middleware rewrite per 02-A: JWT-claims-only checks (no DB reads on page loads), `role:null`/`isOnboarded:false` → `/onboarding/[stage]`, exemption whitelist (DOC-01 list + `_next/*`, static assets, `/verify/[token]`), existing role-prefix map retained, admin mismatch → hard 404. ✅ Done 2026-06-13, commit `688856e`; gate passed (tsc clean, lint 0 errors, 516/516). Notes: matcher extended to `/onboarding/:path*` (kept `/dashboard`, `/api/admin`, daraja). Onboarding lock applies to **page routes only** (APIs self-guard via `requireRole`); `role===null || !isOnboarded` → redirect to `onboardingPathForStage(stage)` (`/onboarding/role-selection|identity-input|verification-upload` — **slug contract AUTH-06 must honour**); a COMPLETED user on `/onboarding/*` is bounced to `dashboardForRole(role)`. Admin mismatch (authenticated non-admin on `/dashboard/admin` or `/api/admin`) → **hard 404** (`hardNotFound`: JSON for API, 404 status for pages) to hide the surface; other role mismatches keep the `/auth/unauthorized` redirect. Unauthenticated → 401 JSON (API) / login redirect with `callbackUrl` (page). Defensive `isExempt` whitelist added. No new tests — middleware/onboarding-loop smoke deferred to QA-03.
- [x] **AUTH-05** — Onboarding APIs: stage-advancing profile-completion routes (Stage 1 role write + `buildRoleDefaults`; Stage 2 role-conditional identity write; Stage 3 verification submissions reusing `verificationStatus: PENDING`); student institutional-email token issue/verify routes + university domain allowlist config; `githubUsername` auto-captured from OAuth profile (never hand-typed). ✅ Done 2026-06-13, commit `8116136`; gate passed (tsc clean, lint 0 errors, 539/539, +23 onboardingSchema tests). Routes (all guard the expected `onboardingStage`, manual session check since role varies): `POST /api/onboarding/role` (ROLE_SELECTION→IDENTITY_INPUT; provider→role enforced — GitHub=STUDENT only; preserves OAuth `githubUsername`), `POST /api/onboarding/identity` (IDENTITY_INPUT→VERIFICATION_UPLOAD; role-conditional schema picks lastName/phoneNumber/county + role sub-doc fields; rejects hand-typed githubUsername), `POST /api/onboarding/verification` (farmer/buyer/lecturer; sets `verificationStatus: PENDING` for farmer/buyer, lecturer stores `facultyCredentialLetterUrl`; →COMPLETED), `POST /api/onboarding/institutional-email` + `/verify` (student; bcrypt-hashed 6-digit pin, 15-min TTL, rate-limited, university-domain allowlist; →COMPLETED). Extracted shared `buildRoleDefaults` (`src/lib/auth/roleDefaults.ts`; register now reuses it — its private copy deleted), added `src/lib/auth/universityDomains.ts` (starter Kenyan list), `sendInstitutionalEmailPin`, and hashed-pin `studentData` fields. **Onboarding COMPLETED ≠ verified** — admin still approves farmer/buyer/lecturer submissions. **Clients must call NextAuth `update()` after each stage** so the JWT refreshes (AUTH-02 handles `trigger==='update'`). Route/state-machine tests deferred to QA-02/QA-03.
- [x] **AUTH-06** — Onboarding UI: SCR-ONB-001…005 (role cards, role-conditional identity forms, student 6-digit pin card with expiry countdown, document upload with progress). ✅ Done 2026-06-13, commit `40bde6e`; gate passed (tsc clean, lint 0 errors, 539/539). Three client pages at the AUTH-04 slugs (`/onboarding/role-selection`, `/identity-input`, `/verification-upload`), each POSTing to the AUTH-05 APIs then calling NextAuth `update()` and pushing to the next stage. Shared `OnboardingShell` (`src/app/onboarding/_components/`) gives card chrome + step indicator; reuses `Input`/`Button` and the unsigned Cloudinary upload pattern. Verification page branches: student → institutional-email pin card w/ live countdown + resend; farmer/buyer/lecturer → document/cert/letter upload. **Note:** `context/FRONTEND.md` does **not** exist (governs website only per DOC-04 anyway); matched the existing app-shell token system (`surface-*`/`text-*`/`t2`–`t6`, `accent-green`). **Known UX gap:** JWT carries no `oauthProvider`, so role cards can't pre-filter by provider — server enforces and the 403 `PROVIDER_ROLE_MISMATCH` is shown inline. UI smoke flow deferred to QA-03.
- [x] **AUTH-07** — Demolition (last): remove CredentialsProvider, password/token fields from `User`, `api/auth/{register,verify-email,forgot-password,reset-password}` + pages + tests, `hashPassword`/`verifyPassword`, login rate limiters. Keep `emailService` (scoped to institutional-email tokens). Migration ruling for existing password users recorded (hard cutover vs dual-run). ✅ Done 2026-06-13, commit `1887a8d`; gate passed (tsc clean, lint 0 errors, **513/513**, `npm run build` green). **RULING: hard cutover** (user, 2026-06-13 — no real users yet; no dual-run/account-linking migration). Removed CredentialsProvider + `authorize` (bcrypt compare, login rate limiter), the 4 `api/auth/*` routes + register/forgot-password/reset-password pages, the now-dead `authSchema` (+ test), User `hashedPassword`/`emailVerificationToken`+Expiry/`passwordResetToken`+Expiry fields + the toJSON strip, and `sendVerificationEmail`/`sendPasswordResetEmail`. **Deviation (user-approved):** kept the bcrypt hashing utils (student pin flow) but **renamed `hashPassword`/`verifyPassword` → `hashSecret`/`verifySecret`**. Login is now Google/GitHub only (surfaces signIn `?error=` codes). **Side effect: fixes OAuth user creation** — `hashedPassword` was `required`, so `User.create` for OAuth users (AUTH-02) would have thrown at runtime; removing the field unblocks it. Middleware `dashboardForRole` repointed to known-good role landings (farmer/buyer/admin have no index route). Auth smoke deferred to QA-03.

## PHASE 4 — UI Builds (against corrected blueprints; every widget backed by a real endpoint)

- [x] **HARNESS-0** — Visual-regression + smoke harness (prerequisite to verifying every UI item). ✅ Done 2026-06-13. Added `@playwright/test` (3-viewport projects: 1440/768/375, dark colorScheme, animations disabled, `maxDiffPixelRatio` 0.01); `playwright.config.ts`; `e2e/support/{auth,global-setup}.ts` + `e2e/smoke.spec.ts`; `.github/workflows/e2e.yml` (mongo:7 service → build → seed → playwright). **Auth strategy:** no real OAuth — global-setup upserts 5 non-destructive `e2e+<role>@umojahub.test` fixtures and mints a NextAuth session JWT per role via `next-auth/jwt` `encode()` (same `NEXTAUTH_SECRET` the server verifies), persisted as Playwright storage state; specs load a role via `test.use({ storageState: authFile(role) })`. **Gotchas solved:** Jest `testMatch` was catching `*.spec.ts` → added `testPathIgnorePatterns` `/e2e/`; root `tsc` → excluded `e2e`/`playwright.config.ts` (Playwright transpiles its own TS, resolves `@/` via root tsconfig paths); webServer readiness must hit a 2xx route (`/auth/login`) since `/` 404s (public site is the separate `site/` project), probed on `127.0.0.1` to dodge the Windows `localhost`→`::1` hang. Verified: 18/18 (5 role landings + unauth redirect × 3 viewports) green against the production build. Existing gate unaffected (tsc clean, lint 0 errors, 638/638). **Per-screen `toHaveScreenshot` baselines arrive with each UI-0x item.**
- [x] **UI-01** — Farmer Fulfillment Pipeline: "Confirm Carrier Handover" time-critical prompt driven by FIX-01 flag; 24-h countdown vs `paidAt`. ✅ Done 2026-06-13; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green, **24/24 e2e** serialized). Fixed all three discovered bugs: (1) page now consumes the server `canConfirmDispatch` flag (added to `IFarmerOrder`); deleted the dead `AWAITING_PAYMENT` local check; optimistic confirm now clears the flag so the CTA disappears post-confirm. (2) Relabelled "Confirm dispatch" → "Confirm Carrier Handover" (list CTA + modal CTA + aria-labels). (3) Added live `HandoverCountdown` component (24 h from `paidAt`, ticks every 60 s, tone escalates green→yellow→red at the 6 h mark / elapsed, using the existing Badge `yellow-*`/`red-*` precedent — no new tokens); shown in the list Actions cell and as a banner in the detail modal with the on-time copy. **API scope (required data dependency):** the farmer branch of `GET /api/orders` (`route.ts`) did NOT return `paidAt`/`confirmedByFarmerAt`/`receivedByBuyerAt`/`buyerPhone`/`fulfillmentType` — the page's interface declared them and the modal dereferenced `fulfillmentType.toLowerCase()` (latent throw). Extended `OrderLean` + the farmer DTO to serialize them (mirrors the buyer branch); `canConfirmDispatch` was already correct from FIX-01. PATCH `/api/orders/[orderId]/status` contract confirmed unchanged: farmer PATCH `{ fulfillmentStatus: IN_FULFILLMENT }` sets `confirmedByFarmerAt` server-side (no payload change needed). **PATCH contract note:** the page still re-sends `IN_FULFILLMENT`; the route guards that the order is currently `PAID && IN_FULFILLMENT` (FIX-01 hardening) and stamps `confirmedByFarmerAt`. **Harness (per "behavior-gated now, pixels deferred" — user decision 2026-06-13):** no committed pixel baseline yet (Windows dev / Linux CI, no Docker to generate Linux snapshots → cross-OS diff would exceed `maxDiffPixelRatio`). Instead `e2e/farmer-orders.spec.ts` asserts the platform-stable contract (server-flag-driven CTA visible, old "Confirm dispatch" label gone, live "16h 0m left to confirm" countdown, modal copy+CTA) with `page.clock.setFixedTime` frozen 8 h after the fixture order's `paidAt` for determinism. `global-setup` now also seeds a deterministic fixture order `E2E-FAR-0001` (PAID + IN_FULFILLMENT + unconfirmed, fixed `paidAt` 2026-01-01T00:00Z, idempotent via `$unset`). **Local-flake note:** parallel workers cold-compile the route on the dev server (>30 s contention) → run e2e `--workers=1` locally; CI is `workers:1` against a built app so it's unaffected. **Pixel-baseline strategy for UI-02…15 deferred to a Linux-generation follow-up.**
- [x] **UI-02** — Farmer Escrow & Settlement Ledger (SCR-FAR-004) on BE-08 + payout-request trigger on BE-02. Copy: "Received by platform — payout pending" (no "escrow" legal claim). ✅ Done 2026-06-13; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 82 pages, **6/6 e2e** serialized). New page `src/app/dashboard/farmer/ledger/page.tsx` (route per SCREEN_INVENTORY) + "Settlement" sidebar nav entry. Fetches both `GET /api/farmers/ledger` (line items + derived balance) and `GET /api/farmers/payout-requests` (request history) in parallel. Renders: 3 balance cards (Received by platform / Committed to payouts / Available to request — the last accent-green), payout-request history table (status→Badge variant map: REQUESTED=warning, APPROVED/PAID=success, REJECTED=error), and a "Payments received" line-item table (ref/item/amount/fulfillment/received). "Request Payout" modal (reuses `Input`/`Modal`/`Button`) POSTs `{ amountKES }` to BE-02 with client-side ≤available + >0 guards, surfaces server errors (PAYOUT_REQUEST_PENDING / PAYOUT_INSUFFICIENT_BALANCE), disabled when available≤0 or an open REQUESTED exists. **Copy-rule compliance:** title "Settlement" (not "Escrow"); explicit "received by the platform — payout pending … no automated disbursement" framing; the word "escrow" never appears in user copy (e2e asserts `getByText(/escrow/i)` count 0). **Deviation:** the checklist's verbatim button label "Initiate Manual Verification & Payout Request" was simplified to "Request Payout" + manual-review helper copy (no blueprint specifies that exact string; clearer UX). **Harness:** `e2e/farmer-ledger.spec.ts` (read-only — opens the form, never submits, so balance stays deterministic) asserts the derived KES 4,000 balance, the E2E-FAR-0001 line item, the copy rule, and the enabled CTA + available-balance hint. `global-setup` now also `deleteMany`s the fixture farmer's withdrawal requests so available==gross==4,000 every run. No new seeding needed — the UI-01 PAID fixture order backs the ledger.
- [x] **UI-03** — Farmer/Lecturer "System Lockout Layer" verification overlays (Q13): farmer fed by `GET /api/farmers`; lecturer fed by `isVerified` JWT claim (AUTH-02 dependency). ✅ Done 2026-06-13; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 82 pages, **48/48 e2e** serialized). New reusable presentational component `src/components/shared/VerificationLockout.tsx` (title/message/tone[pending=amber/action/rejected=red]/optional CTA). **Farmer** (`dashboard/farmer/listings/page.tsx`): now fetches `GET /api/farmers` alongside listings; when `farmerData.verificationStatus !== APPROVED` the page body is replaced by the lockout (rule: "cannot list until verified", FOOD_HUB_ECOSYSTEM_MAP §75/§148) — status→copy map: PENDING="Verification under review" (no CTA), REJECTED="Verification not approved"+profile CTA, UNSUBMITTED/null="Verification required"+submit CTA. Removed the prior always-on static notice (it never actually gated the New-listing button; server enforced but UI didn't). **Lecturer** (`dashboard/lecturer/queue/page.tsx`): the existing inline 'unverified' card was replaced with `VerificationLockout`, and the unverified state is now driven by the **`session.user.isVerified` JWT claim** directly (no API round-trip) per the checklist — the queue API's 403 (`LECTURER_NOT_VERIFIED`) remains as a defensive fallback. **Harness:** added two fixtures — `farmer-unverified` (isVerified false → verificationStatus PENDING) and `lecturer-unverified` (isVerified false JWT claim) — to `e2e/support/auth.ts`; `e2e/verification-lockout.spec.ts` asserts both the locked (unverified sees lockout, no create affordance) and unlocked (verified reaches New-listing / Pending Reviews, no lockout) states. **Fixed:** `smoke.spec.ts` test titles now key off `fixture.key` not `fixture.role` (the new fixtures share role+landing with existing ones → duplicate-title error). No new seeding — fixtures drive everything via verification flags.
- [x] **UI-04** — Group Operations read-only hub (03/08-C): own-group roster + `GroupOrder` state histories; manager variant when `createdBy` matches; **no** interactive member controls; "payment coordinated off-platform" plain-text notice; no per-member payment badges. ✅ Done 2026-06-13; gate passed (tsc clean, lint 0 errors — **dropped to 3** [removed the old group-page exhaustive-deps warning], 638/638 Jest, `npm run build` green — 82 pages, **51/51 e2e** serialized). Rewrote `dashboard/farmer/group/page.tsx` as read-only: **removed the Create Group form** (joining is out-of-band via token redemption = UI-05; the hub mutates nothing); added a **member roster** (was absent — only memberCount shown) with per-member verification badge (Verified/Unverified — surfaces 03/08-C point 4: pre-FIX-06 unverified members stay in rosters), **manager variant** (`createdBy === session.user.id` → "Manager" badge + read-only note that membership is admin-managed), plain-text **"payment coordinated off-platform"** notice, and **GroupOrder histories** via the existing read-only `GroupOrderCard`. No per-member payment badges (paymentStatus deliberately never surfaced). Added a proper role guard + useSession. **API:** `GET /api/groups/[groupId]` now populates `members` with `firstName lastName county farmerData.isVerified` (roster identities; payment fields deliberately not selected) + normalised the membership check to handle populated objects; updated its 4 GET route tests. **Two root-cause bugs fixed (both latent, exposed by this screen):** (1) `GET /api/groups/[groupId]` and `GET /api/groups/[groupId]/orders` populate `User` refs without registering the User schema → `MissingSchemaError` in a cold serverless context (CLAUDE.md gotcha) → added the User-model import to both; (2) the group page dereferenced a populated-null `supplierId` (`(...)._id` on a dangling/deleted supplier ref) → guarded with optional chaining. **Harness:** seeded a deterministic "E2E Cooperative" group (verified farmer = creator + member, unverified farmer = 2nd member) + one GroupOrder in `global-setup`; `e2e/group-hub.spec.ts` asserts the manager badge, off-platform notice, roster identities + verification, order history, and the absence of create/member-control buttons and per-member payment text.
- [x] **UI-05** — "Link Institutional Group Token" single-input component in farmer settings pane, on BE-07 redeem route. ✅ Done 2026-06-13; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 82 pages, **3/3 e2e** serialized). New `src/components/foodhub/LinkGroupTokenForm.tsx` (app-shell tokens, reuses `Input`/`Button`): single token field (uppercased on input, maxLength 32 per `redeemTokenSchema`) → `POST /api/groups/redeem-token`; success shows "You've joined {groupName}." (from the returned `data.groupName`) + clears the field; non-ok surfaces the server message inline (FARMER_NOT_VERIFIED / TOKEN_NOT_FOUND / TOKEN_ALREADY_REDEEMED / TOKEN_EXPIRED / ALREADY_MEMBER / cap). Mounted as a new section in `dashboard/farmer/profile/page.tsx` (the settings pane — there is no separate /settings route; SCREEN_INVENTORY's "settings pane" = profile). **Note:** the profile page is legacy generic-styled (gray tables, pre-design-system); per scope ("never refactor outside the task") the rest was left untouched and only the new component uses app-shell tokens — the legacy profile is flagged for a future migration. Server enforces the FIX-06 verified-farmer gate + atomic single-use; the component only mints the request and reports the outcome. **Harness:** `e2e/link-group-token.spec.ts` submits a bogus token (non-mutating 404 path → deterministic, fixture state untouched) and asserts the control renders + the "Invalid join token." error. No new fixtures/seeding.
- [x] **UI-06** — Buyer Order Detail: "Escalate to Platform Mediation" (48-h client gate) on BE-05 + `UNDER_MEDIATION` alert bar from mediation status (order state untouched). ✅ Done 2026-06-13; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 82 pages, **63/63 e2e** serialized). Extended the existing `dashboard/buyer/orders/[orderId]/page.tsx`: fetches `GET /api/orders/[orderId]/mediation` on load; renders an **UNDER_MEDIATION alert bar** (amber, yellow-* tokens) when the latest mediation is OPEN/IN_REVIEW, with copy that the order status is unchanged (decoupled per Q7). In the IN_FULFILLMENT zone (only when no active mediation), added the **48-h client gate** mirroring the server's `MEDIATION_TOO_EARLY` rule (`canEscalate = now ≥ paidAt + MEDIATION_ESCALATION_HOURS`): before the window → muted "escalate … from {date}" message, no button; after → "Escalate to Platform Mediation" button → inline form (category `<select>` + ≥20-char description `<textarea>`) → `POST .../mediation` ({category, description}), surfacing server errors (MEDIATION_TOO_EARLY / MEDIATION_ALREADY_OPEN / MEDIATION_INVALID_ORDER_STATE); on 201 the returned mediation flips the bar on. Order state machine never touched. **Harness:** the buyer fixture already owns E2E-FAR-0001 (PAID/IN_FULFILLMENT, paidAt 2026-01-01) → escalation-gate tests with a frozen clock (eligible at 2026-01-05, too-early at 2026-01-02), opened read-only (never submitted, so deterministic across the 3 viewport projects). Added a 2nd seeded buyer order **E2E-FAR-0002** with a **dangling farmerId** (so it never enters the fixture farmer's orders/ledger → UI-01/UI-02 unaffected) carrying a pre-seeded OPEN MediationRequest for the alert-bar test; `global-setup` clears the buyer's mediations each run before seeding the canonical one. `e2e/buyer-mediation.spec.ts` (3 scenarios × 3 viewports).
- [x] **UI-07** — Buyer checkout per corrected D01: quantity lock + 90-s poll ticker + explicit timeout/`INVENTORY_LOCK_FAILED` paths (reuse `CheckoutForm` deadline pattern). ✅ Done 2026-06-13; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 82 pages, **72/72 e2e** serialized). Extended `src/components/foodhub/CheckoutForm.tsx` (mounted on `/marketplace/[listingId]`). **Quantity lock** already enforced (stepper `max = quantityAvailable`, "of N avail." indicator) — kept. **90-s poll ticker:** rewrote `startPolling` to a single 1-s timer that updates a visible `remainingSeconds` countdown (with a thin progress bar) every second and polls payment-status every 3rd tick; the awaiting-payment view now shows "Waiting for confirmation · {n}s" instead of a static skeleton. **Explicit timeout path:** the bounded window ends in the `timeout` state (already present) with honest copy + Retry, never an indefinite lock. **Explicit inventory-lock-failed path:** new `inventory_unavailable` state keyed on the real backend codes `ORDER_INSUFFICIENT_STOCK` / `FARMER_LISTING_UNAVAILABLE` (the checklist's conceptual `INVENTORY_LOCK_FAILED` — the shipped atomic compare-and-swap reserve returns `ORDER_INSUFFICIENT_STOCK`); distinct copy + a **Refresh stock** action (reloads the page to re-read current stock) rather than the generic Retry. **Harness:** seeded an AVAILABLE listing (fixed id, stock 25) so the server-rendered detail page mounts the form; `e2e/checkout.spec.ts` uses Playwright `page.route` to intercept the order POST + payment-status (Daraja isn't reachable in the harness) and `page.clock` to run out the bounded window — three scenarios (quantity-lock display, inventory-lock 409 → refresh, 201 → 90s ticker → fast-forward → explicit timeout) × 3 viewports.
- [x] **UI-08** — Student Developer Log Workbench (corrected VIEW-DET-01): 3 hashed document editors w/ auto-save drafts + blocker/AI-usage structured-entry tables; inline 3/3 checklist; finalize button "Finalize & Hash for Review" gated on the 3 docs; three per-doc hash strings displayed. ✅ Done 2026-06-14; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 82 pages, **78/78 e2e** serialized). Enhanced the existing live workbench (`dashboard/student/projects/[id]/page.tsx` + `components/education/DocumentsTab.tsx`) — the 3 docs (problemBreakdown/approachPlan/finalReflection) + 2 structured logs (Blockers, AI Usage tabs) already existed with per-save SHA-256 hashing (`documents` PATCH). Added the corrected-VIEW-DET-01 gaps: **auto-save drafts** (DocumentsTab now debounces 1.5 s after content passes the 50-char min, with Saving…/Saved/Unsaved status; manual Save retained); **per-document hash string displayed** under each editor (`SHA-256 {hash}`, was computed but never shown); **inline 3/3 submission checklist** in the action zone (✓/○ per doc + "{n} of 3 documents"); **two-step finalize** — "Finalize & Hash for Review" (disabled until all 3 docs present) → reveals the **three per-document hash strings** (no compiled hash) → **"Submit for Review"** → `/submit` (plus a "Back to editing" escape). **Harness:** seeded an IN_PROGRESS `ProjectEngagement` (id in `e2e/support/auth.ts`) for the student fixture with all 3 docs (real Node `createHash` SHA-256, matching the backend) → `e2e/developer-log.spec.ts` asserts the 3/3 checklist, finalize → 64-hex hashes + Submit button, and the Documents tab editors + SHA-256 labels (read-only — no typing/submit, deterministic). **Side effect:** the student dashboard redirects to the active engagement, so the student smoke fixture `landing` now points at the project workbench (shared `FIXTURE_ENGAGEMENT_ID` keeps smoke + seeding in sync).
- [x] **UI-09** — Peer Review split-pane workbench: anonymized left pane, structured rubric right pane; preset criteria checkboxes serialize to canonical comment strings (satisfies `peerReviewSchema`); numeric 1–5 inputs. ✅ Done 2026-06-14; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 82 pages, **81/81 e2e** serialized). Enhanced the existing split-pane (`dashboard/student/peer-review/[id]/page.tsx`). The left pane already showed only de-identified content (brief title / track / tier / doc tabs — the GET selects no author) → added an explicit **"Anonymous submission" notice**. The right pane already had **numeric 1–5 ScoreSelectors** (kept). **Core change:** replaced the two free-text comment textareas with **preset-criteria checkbox groups** (`CriteriaGroup`, 5 criteria per dimension); selected boxes serialize via `serializeCriteria` (filtered in declared order = **canonical**, click-order-independent) into the `comments.codeQuality`/`documentationClarity` strings the `peerReviewSchema` requires (non-empty). Submit is now gated on both scores >0 **and** ≥1 criterion per dimension. Submitted view still renders the stored canonical strings. **Harness:** seeded a de-identified reviewee `ProjectEngagement` (dangling author, never populated) + a `PeerReview` ASSIGNED to the student fixture (`$unset` scores/comments each run for a clean form) → `e2e/peer-review.spec.ts` asserts the anonymized notice + doc content (left), both dimensions' 1–5 inputs + criteria checkboxes (right), the disabled-until-valid submit gate, and a client-only criterion toggle (label click — the checkbox input is sr-only). Non-mutating (never submits), so the assignment stays ASSIGNED across viewports.
- [x] **UI-10** — Lecturer Review Workspace (*mask requirement VOID — FIX-08 server-side withholding shipped instead*): 4 score inputs + **4 mandatory ≥50-word comment fields with live word counters** + DENIED-requires-reason logic + post-decision peer-score reveal panel (already wired by FIX-08; this item is now the Phase-4 visual rebuild to blueprint spec only). ✅ Done 2026-06-14; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 82 pages, **84/84 e2e** serialized). **Already built to spec** — `ReviewScoreForm` has the 4 numeric 1–5 ScoreSelectors, 4 `CommentField`s with **live word counters** (`{words}/50`, green when met; `REVIEW_MIN_WORD_COUNT = 50` matches the blueprint), DENIED-reveals-required-reason + submit gated on all-scores + all-comments-≥50 + decision (+reason if DENIED); the page wires the FIX-08 post-decision peer-score reveal with provenance text. UI-10 work = **verification + behavior-gated e2e** per the harness mandate. **Fix (latent, exposed by the seed):** both the lecturer review **detail** and **queue** pages dereferenced a populated-null `studentId` (deleted-author crash) — hardened `studentFullName`/`studentName` with a null guard (mirrors the UI-04 supplier-null fix). **Harness:** seeded a UNDER_LECTURER_REVIEW engagement (dangling author so it never shadows the student fixture's `me`; surfaces "Unknown student") with docs + a blocker + an AI-usage entry → `e2e/lecturer-review.spec.ts` asserts the left work pane, the 4 dimensions, the four live `0/50` counters, the disabled-until-valid submit, and DENIED → required rejection-reason (client-only, never submits). **Note (not in UI-10 scope):** blueprint §2 says the left pane is "anonymized" but the lecturer surface shows the student name elsewhere (queue) — left consistent with the existing lecturer flow; the checklist's enumerated UI-10 requirements do not include anonymization.
- [x] **UI-11** — Supplier directory Read-Only Telemetry View (Q12) in farmer + buyer hubs on existing `GET /api/suppliers`; no registration/creation affordances. ✅ Done 2026-06-14; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 84 pages, **6/6 e2e** serialized). New shared client component `src/components/foodhub/SupplierDirectory.tsx` (reuses the existing `SupplierCard` + `Button`/`ListSkeleton`): fetches `GET /api/suppliers` (the public VERIFIED-only directory) with county + input-category filters and cursor pagination ("Load more"); loading/error/empty/ready states. **Read-only by construction** — administrator-curated framing copy ("suppliers are added after credential verification and cannot self-register"), and NO create/register/edit/add affordances anywhere (the e2e asserts zero such buttons/links). Mounted in **both** hubs via two thin role-guarded page wrappers — `dashboard/farmer/suppliers/page.tsx` (FARMER) and `dashboard/buyer/suppliers/page.tsx` (BUYER) — each rendering the same component; added a "Suppliers" sidebar nav entry for FARMER and BUYER. **Deviation:** no SCR-/FIGMA blueprint specifies a supplier-directory layout (APPLICATION_FIGMA_BLUEPRINT has no supplier node; SCREEN_INVENTORY lists it only as "within farmer/buyer hubs") — matched the existing app-shell token system + reused the shipped `SupplierCard` rather than inventing primitives. `exactOptionalPropertyTypes` required conditional-spreading the optional `SupplierCard` props (contactPhone/physicalAddress/verifiedAt) to avoid passing `undefined`. **Harness:** seeded one VERIFIED supplier "E2E Agrovet Supplies" (Nakuru, FERTILIZER+SEED, KEBS/KEPHIS regs) in `global-setup` (idempotent upsert by businessName); `e2e/supplier-directory.spec.ts` asserts — for **both** the farmer and buyer routes — the heading, the administrator-curated/cannot-self-register copy, the seeded card + its KEBS registration, and the absence of any registration/creation control. Non-mutating (read-only directory), deterministic across the 3 viewport projects.
- [x] **UI-12** — Profile dashboard immutable identity records (Q14): read-only `githubUsername` (OAuth-sourced), institutional fields; admin-correctable carve-out recorded. ✅ Done 2026-06-14; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 86 pages, **6/6 e2e** serialized). **New backing endpoint** `GET /api/users/me` (any signed-in role) — the JWT carries none of the Q14 fields (only id/role/firstName/onboardingStage/isOnboarded/isVerified), so the immutable records are read from the DB: `email` + `oauthProvider` (OAuth anchors), student `githubUsername`/`institutionalEmail`/`institutionalEmailVerified`/`academicRegistrationNumber`, lecturer `departmentAssignment`/`academicStaffId`. Read-only (GET, no mutation path). New shared **read-only** component `src/components/shared/IdentityRecords.tsx` (role-scoped field rows, "Read-only" badge, verified/unverified institutional-email badge, **administrator-correctable carve-out note** — "set when you sign in or during onboarding and cannot be edited here") — **no edit/save affordance by construction** (the e2e asserts zero edit/save buttons and zero input/textarea/select on the page). Mounted via two new role-guarded pages — `dashboard/student/profile/page.tsx` (STUDENT) + `dashboard/lecturer/profile/page.tsx` (LECTURER) — plus a "Profile" sidebar entry for each (neither role had a profile route before; these are exactly the roles whose identity Q14 names). **Scope note:** Q14's enumerated fields (githubUsername + institutional) live only on STUDENT/LECTURER, so the build targets those two; farmer keeps its existing `/dashboard/farmer/profile`, buyer has no Q14 identity fields. The dependency note's "uses AUTH-02 JWT claims" was imprecise — githubUsername/institutional email are not in the JWT, hence the thin DB endpoint. **Harness:** extended `global-setup` `roleData` so the student fixture carries `oauthProvider: github` + githubUsername/institutionalEmail(verified)/regNumber and the lecturer fixture carries `oauthProvider: google` + department/staffId; `e2e/identity-records.spec.ts` asserts — for both profiles — the heading, the Read-only badge, the administrator-correctable note, the seeded field values (+ student "Verified" institutional-email badge), and the absence of any edit/save control or form field. Read-only, deterministic across the 3 viewport projects.
- [x] **UI-13** — Admin: payout-request queue screen (BE-03). ✅ Done 2026-06-14; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 87 pages, **3/3 e2e** serialized). New page `dashboard/admin/payouts/page.tsx` (route per SCREEN_INVENTORY) + "Payouts" admin sidebar entry. Built to the app-shell design system (not the legacy gray admin-queue styling — net-new screen). Fetches `GET /api/admin/payout-requests?status=` (default REQUESTED) with the farmer-identity enrichment + `meta.queueSize`; **status filter tabs** (REQUESTED/APPROVED/PAID/REJECTED) refetch on change; cursor "Load more". Per-row actions are status-driven per BE-03's transition map: REQUESTED → **Approve**/**Reject**, APPROVED → **Mark paid**, terminal states show a status Badge. Decision modal PATCHes `{ requestId, decision, note? }`: **rejection requires a reason** (client guard mirrors the schema refine), PAID note is the optional manual M-Pesa reference, APPROVED is a plain confirm; on success the row leaves the current filter view and the queue count decrements. Server already audit-logs + SMSes every decision. **Deviation:** the rigid 4-col grid collapsed the farmer-name column to zero width at 375px (e2e caught it on mobile) → rebuilt the row as a wrapping flex layout (name/meta block + amount + actions), requested-date folded into the muted meta line. **Harness:** seeded one REQUESTED `WithdrawalRequest` (fixed `_id`, KES 2,500) owned by the **unverified-farmer** fixture — NOT the verified farmer, whose withdrawals are wiped for UI-02 ledger determinism; `$set status` makes it idempotent. `e2e/admin-payouts.spec.ts` asserts the heading, status tabs, the seeded row (farmer name + amount + Approve), the reject-requires-reason client guard, and a submitted decision — with the **PATCH intercepted via `page.route`** so the seeded request stays REQUESTED across the 3 viewport projects (deterministic, non-destructive).
- [x] **UI-14** — Admin: mediation tracking screen (BE-05). ✅ Done 2026-06-14; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 88 pages, **3/3 e2e** serialized). New page `dashboard/admin/mediation/page.tsx` (route per SCREEN_INVENTORY) + "Mediation" admin sidebar entry, app-shell styled. Fetches `GET /api/admin/mediation-requests?status=` (default OPEN; enriched with both parties + the order ref) with status tabs (OPEN/IN_REVIEW/RESOLVED) + cursor "Load more". Each case is a card: order ref + category + status badges, buyer/farmer/order summary line, the buyer's complaint, and (for resolved) the resolution note. Status-driven actions per BE-05's transition map: OPEN → **Start review** (+ Resolve), IN_REVIEW → **Resolve**; the decision modal PATCHes `{ requestId, status, resolutionNote? }` with a **resolve-requires-note** client guard mirroring the schema refine. **Decoupling copy (Q7):** header + IN_REVIEW modal state both that resolving records a note only and never changes the order status or trust score (order state machine untouched server-side). **Harness:** reused the existing UI-06 OPEN `MediationRequest` on order `E2E-FAR-0002` (buyer fixture, dangling farmer → "Unknown farmer", category NOT_DELIVERED) — no new seeding. `e2e/admin-mediation.spec.ts` asserts the heading, status tabs, the seeded case (order ref/category/complaint/parties), the resolve-requires-note guard, and a submitted decision — **PATCH intercepted via `page.route`** so the mediation stays OPEN across the 3 viewports AND the shared UI-06 alert-bar fixture is never mutated (deterministic, non-destructive).
- [x] **UI-15** — Admin: group-token mint console (BE-07). ✅ Done 2026-06-14; gate passed (tsc clean, lint 0 errors, 638/638 Jest, `npm run build` green — 89 pages, **3/3 e2e** serialized). New page `dashboard/admin/group-tokens/page.tsx` (route per SCREEN_INVENTORY) + "Group Tokens" admin sidebar entry, app-shell styled. A mint form (Group ID + recipient phone + expiry preset select — 1/7/14/30 days, mapped to the schema's hours; 14 d default) POSTs `POST /api/admin/group-tokens` (BE-07); on 201 the **minted 8-char code is surfaced prominently** (mono, copy button via `navigator.clipboard`) with the expiry date and a "texted to the recipient · relay manually if SMS doesn't arrive · single-use" note. Client guards require both fields before submit; server errors (404 `DB_NOT_FOUND` for a missing/inactive group, validation) surface inline. **Scope note:** there is no admin group-listing endpoint (`GET /api/groups` is FARMER-scoped to own groups, and adding one is outside UI-15/BE-07 scope), so the group is identified by id — the server validates existence + ACTIVE status and returns the friendly 404. **Harness:** `e2e/admin-group-tokens.spec.ts` asserts the form renders, the required-field guard fires, and a successful mint surfaces the returned code + Copy button — with the **POST intercepted via `page.route`** so no real `GroupJoinToken` row or SMS is created (deterministic, non-destructive across the 3 viewports). **Phase 4 complete — all 15 UI items + HARNESS-0 shipped.**

## PHASE 5 — Verification Gates

- [x] **QA-01** — Unit tests for all new validation schemas + BE routes (adjacent `__tests__/`, per repo convention; validation coverage threshold 95%). ✅ Done 2026-06-13, commit `527c401`. Validation schemas were already covered (every domain schema has a test); added 15 route-test suites for the Phase 2–3 endpoints (payout farmer/admin, mediation order/admin, group-token mint/redeem, ledger, buyer verify/queue/decision, 5 onboarding routes) using the model-mock + `getServerSession` idiom — auth guards, validation, happy path, key error branches. 513 → 607 tests.
- [x] **QA-02** — State-machine tests: payout request lifecycle, mediation lifecycle, token mint/redeem single-use + expiry, onboarding stage transitions. ✅ Done 2026-06-13, commit `c1a3cfa`. Stateful lifecycle suites: onboarding funnel (ROLE_SELECTION→IDENTITY_INPUT→VERIFICATION_UPLOAD→COMPLETED, no-skip/no-repeat across the 3 routes), payout REQUESTED→APPROVED→PAID (+ invalid skip/repeat), mediation OPEN→IN_REVIEW→RESOLVED (+ terminal guard), token single-use (redeem once → second blocked) + expiry. +12 tests.
- [x] **QA-03** — Auth-migration smoke: OAuth sign-in both providers, onboarding lock loop-free (whitelist verified), admin allowlist bootstrap, suspended-user rejection at `signIn` callback. ✅ Done 2026-06-13, commit `c1a3cfa`. `signIn`/`jwt`/`session` callbacks tested directly (new-user creation, Google-only admin allowlist, verified-email requirement → `OAuthEmailUnverified`, suspended → false, block-and-redirect `AccountExists`, `ProviderRoleMismatch`, DB hydration); middleware smoke (login redirect, onboarding funnel + bounce loop-free, admin hard-404 page/API, role-mismatch → unauthorized). +19 tests.
- [x] **QA-04** — Full gate per phase completion: `npm run type-check && npm run lint && npm run test` + `npm run build`. ✅ Done 2026-06-13. tsc clean; lint 0 errors (4 pre-existing warnings); **638/638 tests** across 56 suites; `npm run build` green (81/81 static pages); `test:coverage` passes all thresholds (validation 95%, trust 90%). Pre-existing Mongoose duplicate-index warnings (Order `mpesaTransactionId`/`orderReferenceId`, User `email`) noted — out of scope.

---

### Dependency notes
- Phase 0 blocks nothing technically but blocks *correctly-specified* work — do it first.
- Phase 1 is independent of everything; ship immediately.
- BE-09 ruling resolved 2026-06-13 (option a — buyer-verification queue shipped); AUTH-06 buyer Stage 3 form is unblocked.
- UI-03 (lecturer half), UI-12 depend on AUTH-02 JWT claims.
- AUTH-07 (demolition) is strictly last in Phase 3 and gated on AUTH-03.
